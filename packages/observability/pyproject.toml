[project]
name = "observability"
version = "0.1.0"
description = "Empty stub, filled later"
requires-python = ">=3.13,<3.14"
dependencies = [
    "core",
]

[tool.uv.sources]
core = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"